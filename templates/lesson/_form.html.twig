{% form_theme form 'bootstrap_5_layout.html.twig' %}
{{ form_start(form) }}
    {{ form_widget(form) }}
<button class="form-button" style="width: 195px; display: inline">{{ button_label|default('Сохранить') }}</button>
{{ form_end(form) }}
