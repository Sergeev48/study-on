{% extends 'base.html.twig' %}

{% block title %}{{ 'Изменение урока / ' ~ lesson.name ~ ' / ' ~ lesson.course.name ~ ' / StudyOn' }}{% endblock %}

{% block body %}
    <h5>Изменение урока</h5>
    <div class="d-flex">
        {{ include('lesson/_delete_form.html.twig') }}
        <a class="btn btn-outline-primary ms-2" role="button" href="{{ path('app_lesson_show', {'id' : lesson.id}) }}">Вернуться к уроку</a>
    </div>
    {{ include('lesson/_form.html.twig', {'button_label': 'Сохранить изменения'}) }}


{% endblock %}
